{
  "version": "0.2.0",
  "configurations": [
    {
      "command": "pnpm tauri dev",
      "name": "Run development server",
      "request": "launch",
      "type": "node-terminal",
      "cwd": "${workspaceFolder}/Desktop"
    }
  ]
}
